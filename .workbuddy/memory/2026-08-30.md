# 2026-08-30

## 将截图中的 FIM 配置设为默认值

用户要求把设置页"代码补全模型"红框中的配置作为默认值，方便开箱即用。

已更新以下默认配置：

| 配置项                                    | 旧默认值                | 新默认值                              |
| ----------------------------------------- | ----------------------- | ------------------------------------- |
| `IntelligentCodeCompletion.fim.enabled`   | `false`                 | `true`                                |
| `IntelligentCodeCompletion.fim.apiUrl`    | `""`                    | `http://127.0.0.1:11434/api/generate` |
| `IntelligentCodeCompletion.fim.modelName` | `bigcode/starcoder2-7b` | `deepseek-coder:1.3b`                 |
| `IntelligentCodeCompletion.fim.fimPreset` | `starcoder`             | `deepseek`                            |

改动文件：

- `src/package.json`：VS Code 配置默认值
- `src/core/costrict/auto-complete/fim/types.ts`：`DEFAULT_COMPLETION_MODEL_CONFIG`
- `src/core/costrict/auto-complete/fim/completionEngine.ts`：`getCompletionModelConfig()` 的 fallback 值
- `webview-ui/src/context/ExtensionStateContext.tsx`：webview 初始状态与 fallback 值（两处）

验证：

- `src` tsc --noEmit 通过
- `webview-ui` tsc --noEmit 通过
