## 2026-09-04

### release 1.0.5 提交并推送 origin

- 提交 `fa6f531 release: 1.0.5 — 完善 review 功能：嵌入式审查规范、MISRA/CERT 清单、内置 cppcheck 静态分析`（31 files, +1754/-243），已 push 到 origin（ghproxy 代理，`ce5700e..fa6f531 master -> master`），ls-remote 确认远端 = fa6f531 完全同步。
- 版本号：`src/package.json` 与 `src/bundled-skills/index.json` 均 1.0.4 → 1.0.5（index.json 由 generate-review-builtin.mjs 本地构建写为 commitSha:"local"，保持与上次一致）。
- 本机环境踩坑：
    - **便携 cppcheck 不入库**（与 `src/assets/git/` 同策略）：在 `.gitignore` 加 `src/assets/cppcheck/`；新增 `scripts/download-portable-cppcheck.mjs`（从 GitHub cppcheck-opensource/cppcheck release 下载 MSI → `msiexec /a` 数组传参解包 → 精简 CLI bundle）。SourceForge `/download` 端点不稳（404），GitHub Releases 更可靠。
    - pre-commit/pre-push 钩子跑全仓 `turbo lint`/`check-types`，18 包并行会 OOM/SIGTERM；用 `turbo --concurrency=1` 预热缓存后钩子 FULL TURBO 秒过。
    - `.git/refs/remotes/origin/master` 在本环境写不进去（packed-refs 不更新、loose ref 写入报 ENOENT）→ 本地 status 一直显示 `[ahead N]` 假象，但 ls-remote 证明远端已同步。tracking 引用修复需在普通终端做。
- 还原了 `src/core/task/Task.ts` 的一处纯注释 mojibake 噪音改动（`ï¿½?`↔`ï¿??`），避免进提交。
- 未提交：`_tmp_verify_markers.py`（临时文件，建议删除或自行处理）。

### 收尾修复：ChatRow API 费用徽标也接 developerMode

- 复查 ChatRow/ReasoningBlock 遥测门控完整性：ReasoningBlock 干净（耗时已 gate、思考文案有意保留）；ChatRow 的 speed info 已 gate，但 `api_req_started` 行右侧 `$cost` 徽标（技术遥测）漏接 developerMode——已补上。
- 提交 `179aec4 fix(chat): API 请求费用徽标统一受 developerMode 门控`（ChatRow.tsx + ChatRow.speed-info.spec.tsx 新增 2 测试），lint/check-types 全过，已 push origin（`fa6f531..179aec4`），ls-remote 确认 SYNCED。
- 经验沉淀：凡是数值型遥测（费用/token/耗时/大小）在 UI 上出现，都应查 developerMode 门控是否齐全；建议全局搜 `toFixed(` 与 cost/token 渲染点核查。

### 禁用 TaskHeader：组件瘦身为 return null 空壳

- 需求：不再渲染任务头部（wangj 2026-09-04）。先前尝试「函数体顶部 `return null` + 保留旧代码」行不通——**tsc 会继续类型检查 return 之后的死代码**，TS18048/TS2345/TS2322 全报（todos、task.images、cacheReads/cacheWrites、currentTaskItem、lastUserFeedback 等 possibly undefined）。
- 解决：`webview-ui/src/components/chat/TaskHeader.tsx` 整体瘦身（-495/+12）：保留 `TaskHeaderProps` 接口导出与 `export default memo(TaskHeader)`；组件实现为 `const TaskHeader = (_props: TaskHeaderProps) => null`。
- 关键坑：参数不能省略——组件若声明为无参函数，调用方（ChatView.tsx:1933、TaskHeader.spec.tsx、ContextWindowProgress.spec.tsx）传 props 会报 TS2559（IntrinsicAttributes 无 task 属性）。带 `_props: TaskHeaderProps` 即可让调用点类型契约不变。
- 还原方式：`git checkout -- webview-ui/src/components/chat/TaskHeader.tsx`（HEAD 179aec4 仍是完整实现）。
- 注意：TaskHeader.spec.tsx / ContextWindowProgress.spec.tsx 仍按「header 会渲染」写断言，vitest 跑会挂，未同步改（tsc 编译不受影响）。

### 【更正】TaskHeader 改为按 developerMode 条件渲染（不是删代码）

- 用户否掉了「删死代码瘦身」方案，还原了三个文件（TaskHeader.tsx + 两个 spec）到 HEAD，改为条件渲染。
- 最终实现（webview-ui/src/components/chat/TaskHeader.tsx）：
    - 原组件改名 `TaskHeaderContent`（内部 hooks/JSX 一字未动）；
    - 底部新增外层 `TaskHeader = (props) => { const { developerMode } = useExtensionState(); if (!developerMode) return null; return <TaskHeaderContent {...props}/> }`，`export default memo(TaskHeader)`。
    - 语义：**开发者模式开启才渲染 header，关闭时不渲染**（普通用户看不到 header）。
- 两个硬约束（已写进文件注释）：
    1. 不能「组件顶部 return null + 保留下方代码」——tsc 照样类型检查死代码（TS18048 等）；
    2. 不能在调用部分 hooks 后再提前 return——developerMode 切换时 hooks 数量变化，React 抛 "Rendered fewer/more hooks"。所以必须拆外层判断 + 内层实现。
- developerMode 来源：`useExtensionState()` 解构自 webview 全局 context（webview-ui/src/context/ExtensionStateContext.tsx，`ExtensionStateContextProvider` 持有一份 state，扩展宿主 postMessage 推送 state 并带 seq 防覆盖）；类型定义 packages/types/src/vscode-extension-host.ts:503；UI 开关在设置→通知（NotificationSettings developerMode checkbox），默认 false（SettingsView `developerMode ?? false`）。
- 该语义与既有 TaskHeader.spec（mock developerMode 恒 true）一致，vitest 不受影响。

### 沙箱环境坑：vite emptyOutDir 被 safe-delete 拦截

- 本沙箱跑 `pnpm --filter @roo-code/vscode-webview build` 时，tsc -b 通过、vite transform 6076 模块成功，但末尾清空 `src/webview-ui/build/assets`（397 个旧文件）时被安全策略拦下：`Error: [safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":397,"threshold":50,...}` → Build failed。
- 同类环境噪音：stderr 报 `wmic.exe` 在黑名单被拦（不影响结果）。
- 绕法：`cd webview-ui && pnpm exec vite build --emptyOutDir false`（跳过清目录，bundle 本身 EXIT=0）。正常终端/CI 无此限制，无需改代码。

### TaskHeader 语义确认 + spec 对齐（dev=false 整体不渲染）

- 用户最终语义确认：**developerMode 开启 → 渲染完整 header；关闭 → 不渲染（return null）**（最早的口头方向是反的，已按最终确认实现）。
- 单测对齐：旧用例按「header 恒渲染、dev 只藏遥测行」编写，dev=false 用例会因找不到 "Test task" 而挂。已把 TaskHeader.spec.tsx 的 6 个与 ContextWindowProgress.spec.tsx 的 1 个 dev=false 用例改为断言「header 整体不渲染」（title + 对应行均 absent），避免空过断言。
- 验证：vitest 两个 spec 31 passed / 6 skipped；`tsc -b` 通过。改动范围：webview-ui/src/components/chat/TaskHeader.tsx + 两个 spec。

### 提交并推送 8307ee0（developerMode 门控 TaskHeader + contextManagement）

- 提交 `8307ee0 fix(ui): developerMode 门控 TaskHeader 与 contextManagement 设置页`（4 files：TaskHeader.tsx、TaskHeader.spec.tsx、ContextWindowProgress.spec.tsx、SettingsView.tsx），已 push origin（ghproxy 代理，`179aec4..8307ee0 master -> master`），ls-remote 确认远端 = 8307ee0 完全同步。
- 提交信息不是 -F 传入的原文，而是仓库 commit-msg/format 钩子自动生成（更简短准确），未传入的 index.json（纯格式化 churn）未进提交、也未留在工作区。

### 同步确认：远端 master 已 == 本地 8307ee0（以本地为准）

- 用户要求「本地同步到远端，冲突以本地为准」。实测 `git ls-remote origin master` = `8307ee0` == HEAD，且 `merge-base --is-ancestor origin/master HEAD` = YES → 远端是本地祖先，纯 fast-forward，无冲突可能。
- 实际 `git push origin master` 跑完 pre-push(turbo check-types 全过) 后报 "Everything up-to-date"：因 `8307ee0` 早已在远端（之前 push 过，reflog 有 `update by push` 记录），本次为 no-op。即远端已完整包含 16 个本地提交，符合「以本地为准」。
- 复现旧坑：本沙箱仍禁止写 `.git/refs/`（mkdir/printf/ update-ref 均 ENOENT，`git fetch` 只更 reflog 不更 packed/loose 值）→ `git status` 假显 `[ahead 16]`。真实远端已同步，修复 tracking 需在普通终端跑 `git fetch origin` 或 `git update-ref refs/remotes/origin/master 8307ee0`。
- 流程经验（本环境）：
    - 预热：`pnpm exec turbo run lint check-types --concurrency=1`（30/30，约 12 分钟），之后钩子基本 FULL TURBO；pre-push 的 check-types 个别包 miss 时约 2 分钟。
    - `git commit -F /tmp/xxx` 会报 `could not read log file`——原生 git 不认 Git Bash 的 /tmp；须用 Windows 绝对路径（如 `.git/commit_msg.tmp` + `E:/costrict/...`）。且首次 -F 失败会导致暂存区被还原，需重新 `git add`。
    - hook 偶发 code 134（husky/lint-staged 崩溃），重试即可；提交是否成功以 `git log -1` 实查为准。
