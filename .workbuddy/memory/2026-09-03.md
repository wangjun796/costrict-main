## 2026-09-03

- 解释 mode.ts 中 `apiProvider: "costrict"` 字段的真实作用：非硬开关，仅被 `filterModesByCostrictCodeMode` 读取，且在 provider 为 costrict 时分支被忽略；真正 gate 是 `costrictCodeModeGroup` + `costrictCodeMode`。运行时取 mode 用 `getModeBySlug`，不过滤。
- 上下文窗口：已在上轮将对话中 ContextWindowProgress 显示与 `developerMode` 关联（默认 false 隐藏）。
- 继续将 ChatRow 的 API 请求速度指标（firstTokenLatency/totalDuration/tokensPerSecond）和 ReasoningBlock 的思考耗时也关联 `developerMode`；非开发者模式下默认隐藏这些技术遥测。TaskHeader 折叠态空容器也做了优化。

## 嵌入式 C/C++ 代码审查能力增强

- 为内置 `review` / `security-review` 技能（`src/bundled-skills/{en,zh-CN}/...`）增加嵌入式/C/C++ 与固件安全专项：
    - `review/SKILL.md`：新增 3.7「嵌入式系统与 C/C++」维度 + 「嵌入式与 C/C++ 项目」章节（含 cppcheck 调用指引：先系统 PATH 后内置便携版，`--enable=all --inconclusive --std=... --platform=avr8` 等）。
    - `security-review/SKILL.md`：新增「嵌入式与固件安全」控制（安全启动/固件签名/密钥存储/调试锁定/OTA/通信安全）与渗透测试补充项。
    - 新参考文件：`review/references/embedded-c-cpp-checklist.md`（MISRA C:2012/2023 + CERT C 九大类别详细清单 + cppcheck id 映射表）、`security-review/references/embedded-security-checklist.md`（固件安全清单），均 en + zh-CN 双语。
- 内置便携 cppcheck 2.21.0 到 `src/assets/cppcheck/win32-x64/`（18MB，含 cppcheck.exe + cppcheck-core.dll + OpenSSL + VC++ 运行时 + cfg/platforms/addons，CLI 精简、去掉 Qt GUI）。来源：官方 MSI `cppcheck-2.21.0-x64-Setup.msi` 用 `msiexec /a` 管理性解包（git bash 直接传参易被转义污染，改用 Python subprocess 传 list 解决）。
- 新增 `src/utils/bundledCppcheck.ts`（`getBundledCppcheck`/`buildCppcheckReviewArgs`/`describeCppcheckDataDirs`，镜像 `bundledGit.ts` 模式，但无需运行期解压）；在 `extension.ts` 启动时打日志（接入入口避免 knip 死代码告警）。`src/.vscodeignore` 增加 `!assets/cppcheck/**`。
- 经验：Windows 上 `msiexec /a` 提取 MSI 时 TARGETDIR 参数在 git bash 下会被 `\r\n`/反斜杠转义污染（报 1603 错误），用 Python `subprocess.run([...])` 传参数列表最稳。

## 嵌入式审查增强（续）：mojibake 修复 + cppcheck 路径注入

- 修复 en 版 bundled-skills 的 `??` mojibake（实为 em-dash `—` 的损坏）：`en/review/SKILL.md`(37)、`en/security-review/SKILL.md`(20)、`en/security-review/references/attack-emulation-guide.md`(7)、`security-checklist.md`(1)。zh-CN 各文件本就用正确 `—`，无需处理。全部 `??` 均为「空格??空格」分隔符（grep `[^ ]\?\?` 验证无真实问号），故 `replace_all` 安全。
- 将内置 cppcheck 路径注入经典审查路径的 prompt：`reviewContext.ts` 新增 `resolveCppcheckToolContent(extensionPath)`（返回 `<cppcheck_tool>` 提示，无内置二进制时返回空串）；`codeReviewService.ts` 的 `buildReviewPrompt` 重构后拼接该提示。extensionPath 取自 `clineProvider.contextProxy.extensionUri.fsPath`（与 `createCommentThreadInfo` 同款访问）。
- 注意：云审查路径（`cloudReviewController.ts`）未注入（拿不到 extensionPath），其 AI 仍靠 SKILL 里的 glob 兜底找 cppcheck。

## TaskHeader 展开视图技术遥测行 developerMode 门控（补漏）

- 之前的改动只把 `TaskHeader` 的 ContextWindowProgress 行接了 `developerMode`，但展开视图里的其他技术遥测行（Token 用量、缓存、API 费用、文件大小）以及折叠态的 cost 显示都没接，导致开发者模式关闭时这些仍可见——用户截图反馈后补齐。
- 修复：`webview-ui/src/components/chat/TaskHeader.tsx` 把 Tokens/Cache/ApiCost/Size 四行以及折叠态的 cost 都加上 `developerMode &&` 门控；折叠态的「整理上下文」按钮仍保留（属于用户操作而非纯遥测）。
- 新增 `TaskHeader.spec.tsx` 的 `Expanded task header technical telemetry developer mode gating` describe 块，含 10 个测试覆盖各行 on/off + 折叠态 cost on/off；全套 26 个测试通过。
