# 2026-08-27 工作记录

## AST+LSP Prefix 扩充策略：分析 + 增加开关

### 用户本地改动（AST 跨文件上下文扩充）

- `src/core/costrict/auto-complete/types/index.ts`：PromptOptions 增加 `ast_context: string`
- `src/core/costrict/auto-complete/inlineCompletionProvider.ts`：调用 getAstContext 并塞入 promptOptions.ast_context
- 新增 `src/core/costrict/auto-complete/context/astContextProvider.ts`：解析 import → 解析到工作区文件 → tree-sitter 提取骨架定义 → 拼成上下文串（预算 2000 字符 / 5 文件 / 每文件 80ms 超时）

### 分析结论

- 核心逻辑正确（解析 import、resolveModulePath、parseSourceCodeDefinitionsForFile、预算截断、静默失败兜底）
- 发现 3 个问题：
    1. `ast_context` 为必填字段，但 `completionProvider.test.ts` 两个 promptOptions 字面量没填 → tsc 报错（已修，补 `ast_context: ""`）
    2. getAstContext 无条件调用，每次补全都触发 tree-sitter 解析 + 文件 IO（已门控）
    3. **「LSP」并未实现**——当前只有 tree-sitter AST，命名「AST+LSP」是意向性的；「扩充到 Prefix」实际在服务端（completion-agent）完成，客户端只负责计算并发送 ast_context
- 次要：parseSourceCodeDefinitionsForFile 已自带 `# filename\n` 头，astContextProvider 又加 `// From: modulePath\n`，双重头（冗余，未改）

### 新增开关（本次改动）

- 设置项 `IntelligentCodeCompletion.astContextExpansion`（boolean，默认 false），位置 src/package.json contributes.configuration
- i18n：package.nls.json / zh-CN / zh-TW 新增 `intelligentCodeCompletion.astContextExpansion.description`
- inlineCompletionProvider.ts：`workspace.getConfiguration(configCompletion).get<boolean>("astContextExpansion", false)`，开启才调用 getAstContext，否则 ast_context 传 ""
- 修复 completionProvider.test.ts 两个 promptOptions 补 `ast_context: ""`

### 配置读取约定（本仓库）

- 补全相关 VS Code 设置在 src/package.json `contributes.configuration.properties` 下，键前缀 `IntelligentCodeCompletion.*`，描述走 `%xxx.description%` 指向 package.nls.\*.json
- 读取方式：`vscode.workspace.getConfiguration("IntelligentCodeCompletion").get("xxx", 默认值)`

### 修正（改为 webview 实验性分组）

用户要求选项出现在**插件 webview 设置面板的「实验性」分组**（非 VS Code 原生设置页）。已改：

- 撤销上一版原生设置 `IntelligentCodeCompletion.astContextExpansion`（package.json + 3 个 package.nls），无残留
- 改为实验标志 `AST_CONTEXT_EXPANSION`（ExperimentId = `astContextExpansion`）：
    - `packages/types/src/experiment.ts`：experimentIds + experimentsSchema 增加 astContextExpansion
    - `src/shared/experiments.ts`：EXPERIMENT_IDS.AST_CONTEXT_EXPANSION + experimentConfigsMap（enabled: false）
    - 三语 `webview-ui/src/i18n/costrict-i18n/locales/*/settings.json`：experimental.AST_CONTEXT_EXPANSION.name/description
- 门控接线：`InlineCompletionHost` 增加 `getExperiments()`，`ClineInlineCompletionHost` 从 `provider.getState().experiments` 读取，inlineCompletionProvider 用 `experiments.isEnabled(exps, EXPERIMENT_IDS.AST_CONTEXT_EXPANSION)` 门控 getAstContext

### 关键约定（webview 实验性分组机制）

- 实验项由 `src/shared/experiments.ts` 的 `EXPERIMENT_IDS` + `experimentConfigsMap` 驱动，`ExperimentalSettings.tsx` 遍历 `experimentConfigsMap` 渲染，默认走 `ExperimentalFeature` 复选框
- i18n key 用大写实验键：`settings:experimental.<KEY>.name/.description`（如 `experimental.AST_CONTEXT_EXPANSION.name`）
- 类型三处必须同步：`experimentIds` 数组、`experimentsSchema`、`EXPERIMENT_IDS`/`experimentConfigsMap`（有编译期断言 `AssertEqual<ExperimentId, Keys<Experiments>>`）

## 修复 build 报错（webview tsc 失败）

- 现象：`@roo-code/vscode-webview:build` 报 TS2741/TS2352，因 `astContextExpansion` 加入 `ExperimentId` 后成为 `Record<ExperimentId, boolean>` 必填键，但测试里构造的 experiments 对象字面量/断言没带它
- 修复点：
    - `src/shared/__tests__/experiments.spec.ts`：3 处对象字面量补 `astContextExpansion: false`
    - `webview-ui/src/context/__tests__/ExtensionStateContext.spec.tsx`：对象字面量（L247）与 `toEqual` 断言（L271）各补 `astContextExpansion: false`
- 验证：`cd webview-ui && npx tsc -b` → EXIT=0（类型检查通过）
- 经验：往 `ExperimentId` 加新键后，必须同步更新所有 `Record<ExperimentId, boolean>` 具体对象（含测试断言）；`{} as Record<...>` 空对象强转与 `experimentDefault`（动态生成）无需改

## 自托管 StarCoder FIM 补全：接通配置页 + 修复已有代码

### 背景/架构结论

- completion-agent（Go，`middles/completion-agent`，README 明说"代理程序，转发请求，预处理/后处理"）**不在本地推理**，真正模型推理在云端 `services/code-completions-go`；本地只做 deepseek-tokenizer 截断 + 前后处理。认证在云端（oidc-auth/casdoor + credit-manager），本地 localhost 跳无鉴权。
- 用户需求：自建 StarCoder，用 TS 实现「替代 getFromLLM 的 FIM 请求 + completion-agent 的前后处理」，并加配置页。

### 已有代码（"其他模型生成"）与断点

- 已存在：`src/core/costrict/auto-complete/fim/`（types/preprocess/postprocess/completionEngine/index）、`completionProvider.ts` 已加 FIM 分支、`CompletionModelSettings.tsx`、`SettingsView.tsx` 部分接线、`package.json` 声明 `IntelligentCodeCompletion.fim.*`、`ExtensionStateContext` fim 字段。
- 断点：CompletionModelSettings **从未被渲染**、无 onFim 回调、handleSubmit 未含 fim 字段、webviewMessageHandler 未处理 fim、buildBaseState 未回显、i18n/package.nls 缺失、存储两套（completionEngine 读 VS Code config，webview 保存走 globalState）未打通。

### 修复清单（本次改动）

- **fim 模块 bug**：`completionEngine.ts` import 路径多一层（`../../../base` → `../../base`）；`fim/index.ts` 漏导出 `getCompletionModelConfig`；StarCoder EOS 硬编码 `<｜end▁of▁sentence｜>`（应为 `<|endoftext|>`，改为按 preset 走 `FIM_EOS_TOKENS`）；TGI 请求未 append `/generate` 端点；新增 `topK/repetitionPenalty/doSample` 参数 + `return_full_text:false` + `stripSpecialTokens` 后处理。
- **数据流统一**：fim 配置存 VS Code config（`IntelligentCodeCompletion.fim.*`）。webviewMessageHandler `updateSettings` 用 `FIM_SETTING_KEYS` 映射写 config；ClineProvider `buildBaseState` 读 config 回显到 ExtensionState。
- **类型**：`packages/types/src/vscode-extension-host.ts` 新增 `FimCompletionSettings` interface，`ExtensionState` 与 `updatedSettings` 复用；`ExtensionStateContext` 补 fim 字段。
- **UI**：CompletionModelSettings 补 topK/repetitionPenalty/doSample/stopSequences 输入项（VSCodeTextField 不支持 `type="number"`，改用 `type="text" inputMode=...`）；SettingsView 渲染组件 + handleSubmit 加 fim 字段。
- **配置/翻译**：package.json + 3 个 package.nls 补 topK/repetitionPenalty/doSample；3 语 settings.json 补 `completionModel.*` + `sections.completionModel`。

### 验证

- `src`、`webview-ui`、`packages/types` 三个包 `tsc --noEmit` 全部通过（EXIT=0）。

### 关键约定（本仓库）

- `@roo-code/types` 的 exports `.types/.import` 指向 `./src/index.ts`（源码），改 packages/types/src 无需重 build 即可被 src/webview-ui 类型检查看到。
- 前端设置输入必须绑 `cachedState`（AGENTS.md），保存走 `updateSettings` postMessage → webviewMessageHandler → `contextProxy.setValue`（globalState）或特殊字段写 VS Code config。
